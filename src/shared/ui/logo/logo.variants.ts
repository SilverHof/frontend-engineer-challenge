import { tv } from 'tailwind-variants'

export const logoVariants = tv({
  slots: {
    root: 'flex items-center gap-2',
    text: 'text-[13px] font-semibold tracking-[0.22em] text-[#1E2027]',
  },
})
