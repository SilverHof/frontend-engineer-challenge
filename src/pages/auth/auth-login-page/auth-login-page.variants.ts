import { tv } from 'tailwind-variants'

export const authLoginPageVariants = tv({
  slots: {
    title: 'mb-8 text-[28px] font-bold text-[#1E2027]',
    footerLink: 'font-medium text-[#31A0F0] hover:underline',
  },
})
