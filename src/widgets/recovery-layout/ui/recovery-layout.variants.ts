import { tv } from 'tailwind-variants'

export const recoveryLayoutVariants = tv({
  slots: {
    root: 'flex w-full min-h-screen items-center justify-center p-4',
    card: 'w-full max-w-[480px]',
  },
})
